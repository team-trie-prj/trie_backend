# =========================================================
# trie_backend — vikira (RAG / AI pipeline) dependencies
# Python 3.10
# =========================================================

# --- Web / API ---
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pydantic>=2.6
pydantic-settings>=2.2

# --- DB / ORM ---
SQLAlchemy>=2.0
psycopg2-binary>=2.9        # PostgreSQL driver (prod). SQLite works out of the box for dev.

# --- ① Document parsing / noise filtering ---
pdfplumber>=0.11
python-docx>=1.1

# --- ② Semantic chunking ---
kiwipiepy>=0.17            # Korean sentence splitter (optional; regex fallback if unavailable)

# --- ③ Embedding : BGE-m3 (local, free) ---
sentence-transformers>=3.0
torch>=2.2                 # CPU build on Windows by default (free / offline)

# --- ⑤ Vector DB ---
chromadb>=0.5

# --- Utils ---
numpy>=1.26

# --- Dev / quality ---
ruff>=0.4
black>=24.0
pytest>=8.0
